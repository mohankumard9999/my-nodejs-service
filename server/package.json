{
  "name": "package.json",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:client": "npm --prefix ../client run build",
    "copy:client": "rm -rf client-dist && mkdir -p client-dist && cp -R ../client/dist/* client-dist/",
    "build:spa": "npm run build:client && npm run copy:client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@spotify/web-api-ts-sdk": "^1.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongodb": "^7.0.0"
  }
}
